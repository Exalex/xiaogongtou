# 入口固化方案 · 系统级入口（2026-09-23 方案稿）

> **一句话**：把手机端的「网页入口」升级为「系统级入口」——桌面一点即开、任意界面一键唤起、能说话、有结果回执。
> **状态**：方案稿，**待拍板**（拍板后按第 5 节分阶段落地）。
> **前置阅读**：`项目完整文档.md`（架构/坑位）、`PLAN.md`（一手开发日志）。

---

## 0. 结论先行（TL;DR）

1. **推荐方案：自建一个极薄的原生壳 App（代号「小工头」）**——界面**零重写**（WebView 套现有控制台 v2），要的不是新界面，而是它的**系统身份**：桌面图标、通知、悬浮球、无障碍、语音、系统分享。
2. 分三步攒：**S1 壳（有身份）→ S2 触发器（一键达）→ S3 语音（说话就行）**。
3. **唯一需要新搭的基础设施**：PC 上的 Android 构建链（本机核验：目前没有 JDK / Android SDK）。
4. 后端（`mini_console.py` / `mini_agent.py`）**零改动或极小改动**；入口只调用现有 `/api/*` 接口。

---

## 1. 为什么「网页」注定差一口气（根因）

网页 = 借住在浏览器里的房客：

- 它拿不到系统级能力的**入场券**——通知、悬浮窗、无障碍回调、默认助理、语音按钮、系统分享目标，**这些系统只发给「已安装的 App」**；
- 所以网页版 UI 做得再多分（v2 已是 App 式界面），也永远停在「打开浏览器 → 找到它 → 打字」的框里。

**修正一条旧决策**（2026-09-21 记录：「APK 只是壳，先不做」）：
「壳」的 UI 价值确实不大——但**系统身份的价值只能由壳来承载**。这次要做的不是一个「更好看的界面」，而是一个**「有户口的入口」**。

---

## 2. 「系统级」的分档定义

| 档 | 体验 | 承载手段 | 现状 |
|---|---|---|---|
| S0 网页 | 打开浏览器 → 输地址/找书签 → 打字 | 浏览器 | ← **现在在这里** |
| S1 有身份 | 桌面图标一点即开、全屏无浏览器痕迹、长按图标有快捷方式 | APK 壳（或 PWA） | |
| S2 一键达 | **任何界面**一键唤起（浮球/通知/快捷键）、结果有系统通知回执 | APK + 悬浮窗 + 无障碍 | |
| S3 一句话 | 点一下就说，说完就干活，进度/结果主动通知 | + 系统语音识别 | |
| S4 喊一声 | 说唤醒词即待命（常驻监听） | 唤醒词引擎（可选、后置） | |

---

## 3. 方案对比（4 选 1）

| 方案 | 能到哪档 | 说明 | 判断 |
|---|---|---|---|
| A. PWA 定格（加主屏 + 全屏 + 快捷方式） | S1（半） | 今天就能做；但通知/浮窗/语音/分享**一律拿不到** | 垫脚石，单独做不解决「系统级」 |
| **B. 自建入口 APK（推荐）** | S1–S3（S4 可选） | 需先搭 PC 构建链 + 写 5–6 个源文件；界面零重写 | ✅ 主推 |
| C. 第三方自动化（Tasker 类） | S2/S3 | 付费 + 全在 GUI 里配置 + 依赖第三方生态 | 不推荐（不可脚本化、维护差） |
| D. 接系统助手（小布/Breeno） | ✗ | 厂商封闭：只能「打开某 App」，**无法把口述任务传进来** | 否决 |

> **关于「系统级 AI 唤醒」的诚实结论**：国产 ROM 的系统助手（小布等）是封闭生态，不开放「自定义 HTTP 指令」的能力。所以「唤醒」这件事**只能做在自己的 App 里**——用系统开放给 App 的触发点（助理 role / 无障碍 / 浮球 / 快捷键）把「唤醒」实现出来。这也是方案 B 的核心内容。

---

## 4. 推荐方案设计（B）

### 4.1 总架构（APK 是「门面 + 门铃」，Python 仍是「内勤」）

```
┌─ 手机 ─────────────────────────────────────────────────────┐
│                                                            │
│  ┌─ 入口 APK「小工头」（新增）──────────────┐               │
│  │  桌面图标 · 悬浮球 · 通知 · 语音 · 分享  │               │
│  │  （WebView 全屏壳 → 复用控制台 v2 界面） │               │
│  └────────────────┬───────────────────────┘               │
│                   │ HTTP 127.0.0.1:8900（接口不变）          │
│  ┌────────────────▼───────────────────────┐               │
│  │  控制台 v2・root 常驻（免冻结）          │ ← 不改        │
│  │  mini_console.py                        │               │
│  └────────────────┬───────────────────────┘               │
│                   │ 拉起                                    │
│  ┌────────────────▼──────┐   ┌───────────────────────┐    │
│  │ 小工头 mini_agent      │──▶│ portal（手和眼・免冻） │    │
│  └───────────────────────┘   └───────────────────────┘    │
│  ┌──────────────────────────────────────────────────────┐ │
│  │ 守护 supervisor-loop（新增职责：入口 App 掉线自愈）    │ │
│  └──────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘
```

### 4.2 模块清单（APK 内：全部 Java、纯系统 API、零第三方库）

| 模块 | 作用 | 关键点 |
|---|---|---|
| `MainActivity` | WebView 全屏加载 `127.0.0.1:8900`，复用控制台 v2 全部界面 | 需放行明文 localhost（`usesCleartextTraffic`） |
| `QuickPanel` | 迷你输入面板（一句话输入 + 语音按钮） | 快捷唤起的目标页 |
| `FloatingBall` | 全局悬浮球：任意界面一点即出面板 | `TYPE_APPLICATION_OVERLAY`（用户授一次权） |
| `EntryA11y` | 无障碍服务：承载触发点 + **免冻结护身符**（与 portal 同款机制） | `AccessibilityService` |
| `Notifier` | 常驻入口通知 + **任务完成回执通知**（可带「再跑一次/收藏」） | `Notification` |
| `Voice` | 语音输入：说话 → 文字 → 自动提交 | 系统 `SpeechRecognizer` |
| `ShareIn` | 系统分享目标：在微信等处「分享文本/链接 → 小工头」直接出面板 | `ACTION_SEND` |

### 4.3 触发点菜单（稳 → 待验证）

| 触发点 | 稳度 | 说明 |
|---|---|---|
| 桌面图标 / 长按快捷方式 | ★★★ | 标准能力 |
| 常驻通知点击 | ★★★ | 标准能力 |
| 全局悬浮球 | ★★★ | 需授「显示在其他应用上层」一次 |
| 系统分享目标 | ★★★ | 任意 App「分享 → 小工头」 |
| 无障碍按钮（导航栏） | ★★ | 标准 API，需开无障碍服务 |
| 音量键长按（无障碍快捷方式） | ★★ | **机制已核验存在**（现挂 TalkBack，可换绑）；触发回调行为待真机验证 |
| 长按电源键（默认助理） | ★ | 可注册 `ROLE_ASSISTANT`；ColorOS 是否放行**待验证**（当前系统助理为空、未占用） |
| 唤醒词（「你好小工头」） | ☆ | 可选后置包：常驻监听 + 模型，耗电与工程量都最大 |

### 4.4 模块边界（谁干什么，一句话）

- **入口 APK**：只做 UI / 触发 / 通知 / 语音——**不含任何自动化逻辑**；
- **控制台（root 常驻）**：任务调度、历史、收藏——**接口不变**（`/api/run|status|stop|history|favorites`），后端改动 ≈ 0；
- **portal**：手和眼，不变；
- **守护**：`supervisor-loop.sh` 增加入口 App 的无障碍自愈（复制 portal「修复三部曲」逻辑）。

### 4.5 结果回执（「系统级感」的另一半）

- 任务完成 → **系统通知**（首行 = 结果摘要，点开回到结果页）；
- 通知自带两个动作：「再跑一次」「收藏」；
- 运行中可在通知栏看到进度（可选）。

---

## 5. 构建链与分阶段任务

### 5.1 构建链（唯一新基建）

- 本机现状（2026-09-23 核验）：**无 JDK、无 Android SDK**；
- 需要一次性下载：JDK 17 + `build-tools;34.0.0` + `platforms;android-34`（约 300MB 级）；
- 采用**「无 gradle 五步法」**：`aapt2 → javac → d8 → zipalign → apksigner`，脚本固化为 `scripts/build_entry_apk.sh`——
  **零 gradle / 零 AndroidX / 零第三方仓库依赖**，与项目「纯标准库」哲学一致，以后一键重编；
- 备选：网络不通时用 GitHub Actions 云构建兜底。

### 5.2 分阶段任务（每阶段有可验收场景）

| 阶段 | 内容 | 验收场景 |
|---|---|---|
| **P1 壳包（S1）** | MainActivity + 图标 + 桌面快捷方式 | 桌面点图标 → 全屏无浏览器痕迹 → 跑一个任务 → 结果卡 |
| **P2 触发器（S2）** | 浮球 + 无障碍服务 + 常驻通知 + 分享接收 + 守护接入 | 在任意 App 界面点浮球 → 输入「读电量」→ 25s 内通知出结果 |
| **P3 语音（S3）** | 语音按钮 + 结果通知 | 点浮球 → 按住说「读电量」→ 松开 → 通知报「xx%，充电中」（全程不碰键盘） |
| **P4 深化（可选）** | 默认助理 role / root 音量键手势 / 唤醒词 | 逐个真机验证后决定是否要 |

---

## 6. 风险与对策

| # | 风险 | 对策 |
|---|---|---|
| R1 | 构建链下载不通（Google 源） | 国内镜像 / 本机代理（7897 常开）/ 云构建兜底 |
| R2 | WebView 访问明文 localhost 被拦 | `usesCleartextTraffic` + networkSecurityConfig 白名单 127.0.0.1 |
| R3 | ColorOS 限制浮窗 / 自启 | 浮球由无障碍服务承载（免冻结）；自启复用 Magisk 既有经验 |
| R4 | 自建无障碍被系统清（同 portal 老问题） | supervisor 纳入自愈（修复三部曲复制一份） |
| R5 | 助理 role 不可用 | 非核心，跳过不影响 S3 |
| R6 | 唤醒词常驻耗电 | 放 P4 可选；默认不做 |
| 安全 | 新增攻击面 | 入口只访问 loopback:8900；不引入任何外网权限面 |

---

## 7. 真机核验记录（2026-09-23 方案前侦察）

- 手机在线：`192.168.3.170:5555`（一加 9 / LE2110 / Android 14）；
- 控制台 root 常驻在线：supervisor pid 22564 + mini_console pid 26758；`127.0.0.1:8900` LISTEN；
- portal 在线：`:8080` LISTEN（pid 20722）；
- **无障碍快捷方式机制存在**：`accessibility_shortcut_target_service` 当前挂 TalkBack → 可换绑自建服务（触发行为待验证）；
- **默认助理当前为空**（`assistant` 未占用）→ `ROLE_ASSISTANT` 可行性待验证；
- PC 侧：无 JDK / 无 Android SDK（构建链是唯一新增前置）。

---

## 8. 拍板与实施记录（2026-09-24 凌晨）

**用户拍板**：2026-09-23 23:10「可以，你开始做吧」→ 全量放行（P1+P2+P3 一次做完）。

### 8.1 已交付（真机验证通过）

| 项 | 状态 | 证据 |
|---|---|---|
| 构建链（JDK17 + SDK35 + 无 gradle 五步法） | ✅ | `entry/build.py` 一键出包（65.5KB debug 签名 APK） |
| P1 壳（桌面图标 + WebView 全屏加载控制台） | ✅ | 桌面出现「小工头」图标；壳内完整渲染控制台 v2（`logs/entry_shell_p1.png`） |
| P2 浮球（无障碍 overlay 全局悬浮） | ✅ | 任意界面可见（`logs/entry_ball_home.png`） |
| P2 悬浮输入面板（点浮球就地展开） | ✅ | 面板 + 键盘自动弹出、可打字；点空白关闭（`logs/entry_overlay_panel.png`） |
| P2 待命通知 | ✅ | `dumpsys notification` 见 id=1001「小工头待命」 |
| P2 守护联动（a11y 被清自动补回） | ✅ | `supervisor-loop.sh` v3 已部署并重启验证 |
| P3 语音（代码就绪） | ⏳ | 待日常使用验证（长按「说话」按钮） |
| 端到端任务（提交→结果通知） | ⏳ | 待不用手机时验证 |
| **调试模式**（网页 🐞 开关 + 网页「🔗 链路」+ 手机半透明链路浮窗 + 长按浮球开关） | ✅ | `logs/debugoverlay_done.png`、`logs/console_ui_trace.png`（2026-09-24 凌晨实证） |
| **经验库**（Jev 外挂 skill：成功存轨迹 → 同类任务检索注入） | ✅ | 第二次同任务 trace 显示「已注入经验 1 条」；Jev 首步置信度 0.38→0.85 |

### 8.2 关键发现（新增坑位）

1. **ColorOS 对 sideload 应用会「o-stop」**：`am_kill … due to o-stop(40)` —— 实测「从浮球启动 Activity（即后台启动 Activity）」后 1 秒内进程被杀、a11y 服务同时被移出启用列表。
   **对策（已实现）**：面板改为**无障碍 overlay 窗口就地展开**（`TYPE_ACCESSIBILITY_OVERLAY`），不启动任何 Activity → 实测点击浮球、展开面板、键盘输入全程进程存活、无 o-stop。
2. **ColorOS 会清理 sideload 应用的 a11y 服务**（启用列表里静默消失）→ supervisor v3 每 5 分钟自检补回（保留 portal，不相互踢）。
3. **无障碍 overlay 窗口可正常使用输入法**（EditText + 键盘弹出实测通过）——浮窗输入的关键前提。
4. **安装方式**：`adb push` + `su -c pm install -r`（root 安装绕过 ColorOS「安装确认页」；首次 adb install 会被拦）。
5. **保活加料**：`deviceidle whitelist +com.xiaogongtou.entry` + `appops RUN_IN_BACKGROUND/RUN_ANY_IN_BACKGROUND allow`。

---

## 9. 固化清单（已定稿 2026-09-24）

- [x] 入口形态：自建入口 APK「小工头」（`entry/`，包名 `com.xiaogongtou.entry`），WebView 复用控制台界面
- [x] 应用名 / 图标：**小工头**（深色圆角方 + 白「工」+ 右下绿点）
- [x] 触发点优先级：① 桌面图标 ② 点浮球 → 悬浮面板 ③ 待命通知 ④ 无障碍按钮 ⑤ 系统分享（ACTION_SEND）
- [x] 守护覆盖：supervisor v3 = console 自愈 + portal 自愈 + 入口 a11y 补回
- [x] 构建 / 更新：`entry/build.py`（无 gradle 五步法）；装机 = `adb push` + `su -c pm install -r`
- [x] **调试模式**：网页 🐞 开关（`/api/debug`）+ 「🔗 链路」浮层（`/api/trace`）+ 手机半透明调试浮窗（`DebugOverlay`）+ 长按浮球快捷开关
- [x] **音量键快捷键**（默认开、可关）：按一下唤起**语音速记条**（自动打开输入法语音输入）/ 再按执行 / 长按正常调音量；网页 ⌨ 开关（`/api/hotkey`）
- [x] **语音速记条**（`VoiceBar`，2026-09-24）：轻量输入条 + 键盘 + 三级自动点语音键（a11y 节点 → portal 坐标注入 → 无障碍手势）；面板「说话」按钮同链路
- [x] **未来感面板 UI**：深色玻璃卡片 + 霓虹绿发光描边 + 脉冲点 + 入场/呼吸/淡出动画
- [x] **经验库（Jev 外挂 skill）**：成功存轨迹（`~/bridge/skills/examples.json`）→ 同类任务检索注入 prompt
- [x] 文档同步：本方案 + `项目完整文档.md` + `手机入口使用说明.md`
- [ ] 可选深化（未做）：S4 唤醒词、默认助理 role、音量键手势

---

*本方案由 2026-09-23 会话生成；与 `项目完整文档.md`、`PLAN.md`、`手机入口使用说明.md` 配合阅读。*
