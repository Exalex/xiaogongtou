# 手机自动化 · 使用说明

一个装在你手机里的"小工头"：**说一句话，手机自己动手干活**。全程不需要电脑。

---

## 三种打开方式（任选）

| 方式 | 怎么用 | 什么时候用 |
|---|---|---|
| **桌面图标「小工头」** | 点图标 → 全屏控制台界面 | 想看历史/收藏、认真派活 |
| **悬浮球「工」**（推荐） | 任意界面点右侧圆球 → 就地弹输入面板 | 正在看别的 App，随手派个活 |
| **通知栏** | 下拉通知栏点「小工头待命」 | 找不到浮球时 |

## 怎么用（3 步）

1. 用上面任一方式唤起输入面板（或打开 App 用底部输入框）
2. 打字说出任务，选一个模式，点「发送」（想用语音：点面板的「说话」，或**按一下音量下键**）
3. 它开始干活（**屏幕会被自动操作，别碰手机**）；干完看**通知**里的结果；也可打开 App 看历史和结果大卡

## 三种模式

| 模式 | 用法 | 说明 |
|---|---|---|
| **智能**（默认） | Jev 起步，卡住自动换 qwen | 推荐日常用；实测"卡住→自动升级"25 秒出结果 |
| **极速** | 只用 Jev（最快） | 简单任务，每步 1 秒左右 |
| **深度** | 直接用 qwen | 复杂任务 / 兜底；会慢一些（首次调用可能等 1-2 分钟） |

## 快捷键（默认开）

- **按一下「音量下键」** → 弹出**语音速记条**：键盘自动弹出，并**自动打开输入法的语音输入**（本机 = 搜狗）→ **直接说话**，文字自动填进输入框 → **再按一下** → 直接执行
- 想改成打字：语音条本身就是输入框，直接敲字即可；不想要了点条上的「×」
- **长按「音量下键」** → 正常降低音量（原来的用法不受影响）
- 不想要这个快捷键：网页顶栏点 **⌨** 关掉（关掉后音量键完全恢复原样）

## 调试模式（想看看它怎么想的）

- **怎么开/关**：网页顶栏点 **🐞**；或（手机任意界面）**长按浮球**约 0.6 秒
- 开了以后，手机屏幕下方会出现**半透明调试窗**：每一步的「发给模型的 / 模型回什么 / 做了什么动作 / 结果」实时滚动
- 想细看：网页的结果卡里点「**🔗 链路**」，看完整链路（含置信度、每步耗时）
- 它会**自动积累经验**：同一个任务干过第二次，会参考上次的做法（trace 里会显示「已注入经验 N 条」）

## 没反应 / 找不到浮球怎么办？

| 情况 | 处理 |
|---|---|
| 提交时提示"控制台离线" | 等 1 分钟（守护会自动拉起）；或打开一次 Termux 跑 `bash ~/start-console.sh` |
| **悬浮球不见了** | 守护每 5 分钟自动补回；急用就打开一次「小工头」App |
| 任务报 portal 不通 | 守护会自动修（≤5 分钟） |
| 想重跑 / 收藏任务 | 打开「小工头」App → 历史记录里点 ↻ 或 ☆ |

## 小抄（在 Termux 里敲）

| 我要干嘛 | 命令 |
|---|---|
| 启动 / 重启控制台 | `bash ~/start-console.sh` |
| 停止控制台 | `bash ~/start-console.sh stop` |
| 看服务日志 | `cat ~/bridge/out/console.log` |
| 看任务记录 | `ls ~/bridge/runs/`（每次任务一个 .out） |
| 手机重启后 | 无需操作：控制台与入口会自动恢复；浮球没出来就打开一次「小工头」 |

## 边界（先知道，免得困惑）

- **需要联网**：Jev 走公网；qwen 走家里局域网服务器（192.168.3.75）——出门在外用"智能/极速"没问题，"深度"需连家里 WiFi。
- **任务运行期间手机屏幕会被自动操作**（点按/滑动），属正常现象；跑的时候最好别动手机会打断它。
- 任务起点建议留在**主页桌面**（防桌面伪 Settings 图标诱骗）。

## 实现一览（给想深挖的你）

- **「小工头」入口 App**（`com.xiaogongtou.entry`）：桌面图标 + 悬浮球 + 输入面板 + 通知；源码在项目 `entry/`（纯 Java，无 gradle 构建）
- `mini_agent.py` —— 手机里的"小工头"（看屏幕→问脑子→动手 循环；三模式引擎）
- `mini_console.py` —— 控制台（本机网页服务 `127.0.0.1:8900`，App 壳里显示的就是它）
- `start-console.sh` —— 启动脚本；`~/.bashrc` 里有一行自动拉起
- `supervisor-loop.sh`（root 常驻）——三合一守护：控制台 / portal / 入口 App 的 a11y 服务
- 任务日志与截图都在 `~/bridge/` 和手机桌面操作记录里
