# 开发笔记 · 通宵迭代（2026-09-24）

> 任务：①创建 GitHub 版本 → ②持续迭代（每轮真机操作手机、发现问题、下一轮改进 UI/功能/架构）→ ③全过程记录到本文档。
> 规则：不打断、不询问，测试 → 反馈 → 改进 循环。

## 迭代看板

| 轮次 | 时间 | 主题 | 状态 | 验证 |
|---|---|---|---|---|
| 1 | 02:0x | GitHub 发布 + 密钥脱敏 | 进行中 | 构建/推仓/真机任务 |
| 2 | | | | |

---

## 轮 1 · GitHub 版本 + 密钥脱敏

**背景**：GitHub 发布前全仓扫描发现真实密钥（TypeSafe Jev key、portal token）硬编码在多处。

**动作**：
1. 手机端 `mini_agent.py`：`JEV_KEY` / `PORTAL_TOKEN` 改为 **`~/bridge/secrets.json` 读取**（不进 git），环境变量优先。
2. `jev_probe.py` / `pc_portal.py` / `portal_probe.py`：密钥默认值改为空 + 环境变量。
3. 入口 APK `Api.java`：portal token 改为引用 `Secrets.PORTAL_TOKEN`；`Secrets.java` 由 **build.py 从 `entry/local_secrets.json`（gitignored）生成**到构建目录（不进源码树）。
4. 文档脱敏（`项目完整文档.md` 两处示例）。
5. 新增 `.gitignore`（密钥、构建产物、截图 logs、.workbuddy 等）。
6. 新增 `README.md`（架构图 + 快速开始 + 文档索引）。
7. `gh repo create` 发布。

**验证**：（构建 / 真机任务 / push 结果 —— 补记于下方）
