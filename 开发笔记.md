# 开发笔记 · 通宵迭代（2026-09-24）

> 任务：①创建 GitHub 版本 → ②持续迭代（每轮真机操作手机、发现问题、下一轮改进 UI/功能/架构）→ ③全过程记录到本文档。
> 规则：不打断、不询问，测试 → 反馈 → 改进 循环。

## 迭代看板

| 轮次 | 时间 | 主题 | 状态 | 验证 |
|---|---|---|---|---|
| 1 | 02:07 | GitHub 发布 + 密钥脱敏 | ✅ | 真机任务通过（jev=2）、`gh repo create Exalex/xiaogongtou` 推送成功 |
| 2 | 02:13 | 屏幕变化反馈 + 无效点击拦截（P0-2 前半） | ✅ | 微博任务复测：无效点击被 BLOCK（但暴露新问题） |
| 3 | 02:18 | wait 选项 / 阶段检查 / block 警告升级 | ✅ | 部署生效；发现"感知盲区"根因 |
| 4 | 02:22 | **感知补盲**：uiautomator root 桥（架构级） | ✅ | 微博任务 8 步 done（首胜），但暴露"done 幻觉" |
| 5 | 02:26 | **防幻觉复核**（done 验证） | ✅ | 微博任务 19 步 done，结果真实、复核通过（微博热搜首胜） |
| 6 | 02:31 | Jev 400 修复（criteria 截断）+ 元素限流 + 强制补盲（BLIND_APPS） | ✅ | 400 清零；微博前3任务出现"诚实失败"（不再幻觉） |
| 7 | 02:37 | **感知换源 + 操作容灾**（架构级）：小工头 App 树导出（替代 uiautomator）+ root 桥 input 容灾 | ✅ | 桥树 24 次调用、portal 0 掉线、uiauto 0 调用；新增"滚动迷路"检测 |
| 8 | 02:44 | 滚动迷路检测（scroll_streak≥6 强制升级） | ✅ | 设置-电池任务 4 步成功「85%」；计算器 8 步成功「12+34=46」 |
| 9 | 02:47 | **登录页安全护栏**（检测+禁止登录操作）+ 运行中通知进展更新 | ✅ | 微信任务：25 步烧光+乱点验证码 → **4 步安全停止**（诚实报告"需先登录"）；通知栏显示"正在干活…（第 N 步）" |
| 10 | 02:55 | **失败教训库**（failures.json：失败入库 + 同类任务注入"别再做"） | ✅ | 微博任务失败 → 教训入库 ✓ → 下次运行注入「2 条」✓ |
| 11 | 03:03 | 四任务连测 + 语音闭环回归 | ✅ | 计算器 56+78=134 / 电池 94% / 浏览器搜索 25°/32° / 天气 22℃ 全过；语音提交闭环 ✓ |
| 12 | 03:10 | 语义复核 + 桥守护 + 微博封存 | ✅ | qwen_verify 加"内容类型"检查；supervisor 看护 xgt-bridge；微博转诚实失败策略 |
| 13 | 03:16→08:01 | **自我进化**（成功后自动沉淀 App 要点）+ GitHub 补推 | ✅ | 闹钟任务 done「07:00」；apps.json 自动新增闹钟要点；6 提交全部上线 |
| 14 | 08:04 | 日历任务泛化 + 自我进化二次验证 | ✅ | 4 步（纯 Jev）done「今天没有日程」；apps.json 自动新增日历要点 |
| 15 | 12:00 | **开工规划（qwen 理解+计划）** + 包名修复 + 滑动守卫 + 复核强化 | ✅ | 开盘啦 6 步 done（真实热点 6 条）；华尔街见闻 18 步 done（真实头条 4 条） |
| 16 | 13:00 | **微博热搜感知攻坚**（树深度+label 截断 / qwen 同源感知 / 经验库卫生） | ✅ | 25 步失败 → 10 步 done（真实热搜）；APK + agent 双修复 |
| 17 | 14:22 | **微博真榜路径修正**（经验坐标注入 / 热度降序排名提取 / 列表全量答案 / 手册卡保护） | ✅ | 6 步纯 Jev（坐标 conf 0.94/0.91）；答案与 RankPage 真榜 1-5 逐条一致 |
| 18 | 15:20 | **命中/记账/加速 实测**（连测 A/B/C + 全天数据拉通 + 三处补强） | ✅ | A 34.4s/6步；B 55.2s/13步（起点乱）；C 93.9s/7步（portal 掉线自愈）；错题本悖论修复 |

---

## 轮 6 · Jev 400 修复 + 强制补盲（02:31）

**发现**：
1. uiautomator 补盲树 436/588 元素 → **Jev criteria 超 255 上限 → HTTP 400** → 每步 fallback qwen。
2. 微博任务在"portal 元素 ≥8 但内容区依旧缺失"时**不触发补盲** → qwen 总结基于残缺树 → 结论"未显示热搜内容"（误判）。

**动作**：
- `jev_decide` 入口截断 >180 元素；`bridge_elements` 限流（可点 80 + 文本 40）。
- 新增 **BLIND_APPS**（内容区常被 a11y 过滤的 App 名单，内置微博；`~/bridge/skills/blind_apps.json` 可扩展）→ 命中即**强制补盲**。

**验证**：400 清零；微博前3任务 → **诚实报告"未显示热搜内容，无法查看"**（不再编造；"无法完成"是合法结局）。

## 轮 7 · 感知换源 + 操作容灾（02:37）★★★

**根因确认（重要）**：**`uiautomator dump` 会触发系统解绑 portal 的 a11y 服务**（unbound→destroyed→reconnect，约 10 秒 socket 停摆；实测复现）。此前多次 portal "ConnectionRefused" 掉线疑似由此触发。

**架构替换（不再用 uiautomator）**：
1. **小工头 App 树导出**（`EntryA11yService`）：
   - 新增 `flagIncludeNotImportantViews`（**让"不重要"视图可见** —— 与 uiautomator 同等的树质量）
   - `getWindows()` 全窗口遍历 → 元素 JSON（[idx,cx,cy,label,clickable]）写 `外部私有目录/tree.json`
   - 触发协议：root 写 `tree_req`（seq）→ App 250ms 轮询发现 → 导出
   - **实测**：微博场景 26 元素、**含信息流正文与搜索建议**（此前 portal 只有 5 个导航元素）
2. **root 桥升级 `xgt-bridge.sh`**：`tree`（取树）+ `tap/swipe/key/open`（**portal 容灾通道**）
3. **agent 侧**：`bridge_cmd` / `bridge_elements`；Portal 的 tap/swipe/key/open_app **失败自动走桥**；portal 整体不可用时 → 用桥树 + 伪 state 继续任务（不中止）。

**验证**（任务 0924-024105）：
- **uiauto 调用 0、portal 错误 0、Jev 400 = 0**（三项稳定性指标全绿）
- 桥树调用 24 次（portal 5 → 桥树 26 元素）
- 任务本身失败：Jev 连续 25 步滚动未 done（页面"回到顶部"浮动按钮遮挡了"发现"导航位 → Jev 读不到"发现"）→ 新发现"**滚动迷路**"模式：滚动改变屏幕哈希 → 不触发常规无进展 → 不升级 qwen → 25 步烧光。

## 轮 8 · 滚动迷路检测（02:44）

**动作**：`scroll_streak`——连续滚动/滑动 ≥6 次（且未 done）→ 强制升级 qwen（与无进展升级同路）。

**验证**：（设置-电池任务测试中）

---

## 轮 1 · GitHub 版本 + 密钥脱敏（02:07）

**背景**：GitHub 发布前全仓扫描发现真实密钥（TypeSafe Jev key、portal token）硬编码在多处。

**动作**：
1. 手机端 `mini_agent.py`：`JEV_KEY` / `PORTAL_TOKEN` 改为 **`~/bridge/secrets.json` 读取**（不进 git），环境变量优先。
2. `jev_probe.py` / `pc_portal.py` / `portal_probe.py`：密钥默认值改为空 + 环境变量（补了 `import os`）。
3. 入口 APK：`Api.java` 的 portal token 改为引用 `Secrets.PORTAL_TOKEN`；**`Secrets.java` 由 build.py 从 `entry/local_secrets.json`（gitignored）生成**到构建目录（源码树不含密钥）。
4. 文档脱敏（`项目完整文档.md` 2 处）。
5. `.gitignore`（local_secrets / build / dist / logs / apk / .workbuddy）。
6. `README.md`（架构图 + 快速开始 + 文档索引）。
7. **发布**：`gh repo create xiaogongtou --public --push` → https://github.com/Exalex/xiaogongtou

**验证**：手机侧部署 `~/bridge/secrets.json` + 新 APK；跑「回到桌面」任务 → `done`（jev=2）——脱敏后 Jev 与 portal 链路均正常。

## 轮 2 · 屏幕变化反馈 + 无效点击拦截（02:13）

**背景**（微博案病根）：qwen 连点同一坐标 5 次不自知——它收到的反馈只有"执行成功"，**不知道"屏幕没变"**。

**动作**（`mini_agent.py`）：
1. 每步开头用"屏幕 hash 对比"**补记上一步效果**："（系统补充）上一步动作已执行，但屏幕没有任何变化——可能没点中/无效"。
2. **无效点击计数**：同一 tap 连续 2 次"无效果" → 并入硬阻断体系（拒绝执行 + 强反馈）。

**验证**：微博任务复测 → 无效点击确实被 BLOCK（step 9-25 全部被拒）✓；**但暴露新问题**：qwen 被 BLOCK 后仍反复提交同一动作 17 次（不改策略）。

## 轮 3 · 加载等待 + 阶段检查 + 警告升级（02:18）

**动作**：
1. **Jev 增加 `wait` 选项**（"页面正在加载"场景）——乱点治不了，先学会等。
2. **每 6 步"阶段检查"提示**："请先看屏幕：任务要求的信息是否已出现？已出现 → 立即 done"。
3. **连续被报复 ≥3 次**时警告升级："坐标已禁用；若认为任务已完成请直接 done"。
4. 升级 qwen 的交接提示追加"若屏幕上已有信息，请直接 done"。

**验证**：微博任务复测 → qwen 开始用 wait/swipe/返回/open_app 组合尝试（比死循环进步）；**但任务仍失败**——复盘发现：**qwen 说"内容区域空白"不是它笨，是真的看不到**！

## 轮 4 · 感知补盲：uiautomator root 桥（02:22）★★★

**根因发现**（本夜最大发现）：
- 同一时刻对比：**uiautomator dump 能看到微博信息流内容**（163 节点 / 16 文本），**portal 的 a11y 树只有 5 个节点**（只剩底部导航）。
- 原因：**portal 的 AccessibilityService 未开 `FLAG_INCLUDE_NOT_IMPORTANT_VIEWS`**（capabilities=161 里无 0x2），微博标记为"不重要"的内容区被过滤。
- 结论：**agent 一直以来是"瞎"的**（读不到信息流内容）——不是模型笨。微博案、部分"内容读不到"类失败的真正根因。

**架构方案（不动 portal）**：新增 **root 桥 `uidump-bridge.sh`**：
- root 常驻小脚本：轮询 `~/bridge/.uidump_req` → `uiautomator dump` → 写到 `~/bridge/.uidump.xml`（chown 给 Termux）。
- `mini_agent.py`：每步若 portal 树 **<8 个元素** → 自动请求补盲，用 uiautomator 完整树替换（坐标体系相同，可直接点击）。

**验证**：微博任务（portal 恰好掉线 + 树为空）→ **首次 done！8 步**（portal 0 → uiautomator 11 元素）。**但**：结果"热搜前5条：中国男篮vs日本男篮×5"经比对 `.uidump.xml` 铁证，**屏幕上是"推荐"信息流，没有任何男篮内容——qwen 编的（幻觉）**。

## 轮 5 · 防幻觉复核：done 验证（02:26）

**背景**：轮 4 的"假成功"——qwen 未完成任务但输出编造答案。
**动作**：
1. 新增 `qwen_verify(task, summary, screen_text)`：done 时让 qwen 自查"结果的关键信息能否在屏幕上找到依据"。
2. **驳回机制**：复核不通过 → 拒收 done 并把驳回理由回传引擎（要求继续操作，或如实报告"无法完成：原因"）；最多驳回 2 次防死循环。
3. "无法完成"开头的 summary 直接放行（**如实失败是合法结局**）。

**验证**：
- 任务 0924-022359：8 步 done，但结果"中国男篮vs日本男篮×5"经 `.uidump.xml` 铁证比对 = **编造（当时屏幕是微博推荐流，无任何男篮内容）** → 暴露"done 幻觉"。
- 任务 0924-022714（防幻觉版+阶段检查）：**19 步 done，结果真实**："花少2搬箱子楼梯实际长这样 / 杨幂骗了全网 / 腾势Z9S上市 / 武大7400多退休教师年领9亿 / 中国体操男团金牌"——**经防幻觉复核通过**（屏幕上确有此依据）。
- **微博热搜任务：从"25 步失败×3 次"到"19 步真实完成"** ——感知补盲 + 阶段检查 + 答案机制 + 防幻觉复核组合生效 ★
- 遗留：Jev 在 436/588 元素时 **HTTP 400（criteria>255 上限）** → 已修（截断 180 + 补盲元素限流 80+40），待验证。

## 轮 9 · 登录护栏 + 通知进展（02:47）

**计算器任务（泛化验证）**：`打开计算器，算一下 12+34 等于多少` → **8 步成功「46」**（含弹窗处理；纯 Jev；教科书级逐键操作）✓。

**微信任务（安全事件）**：`打开微信看最近聊天` → 微信未登录（登录页）→ **agent 乱点"获取验证码"（危险！）** 且 25 步烧光。
**动作**：
1. **登录页检测**：屏幕出现 ≥3 个登录关键词（登录/验证码/密码…）→ 注入"不要尝试登录；无法完成请 done 如实报告"。
2. **危险点击护栏**（执行层）：点击目标含"验证码/密码" → 直接拦截（不执行）。
3. Jev criteria 与 qwen SYSTEM_PROMPT 均加安全规则（禁止操作登录/验证码类元素）。
4. **运行中通知进展**：通知栏实时显示"正在干活…（第 N 步）+ 最新动作"。
**验证**：微信任务 → **4 步 done「无法完成：需要先登录」**（安全、诚实）✓；通知栏实测显示"第 7 步"✓。

## 轮 10 · 失败教训库（02:55）

**设计**（回应最早的架构诉求"失败的经验要注入"）：
- `save_failure`：任务失败（步数用尽/中止）→ `~/bridge/skills/failures.json`（任务+原因+最后动作链；去重置顶，上限 30）。
- `load_experience` 改为**同时读 examples（成功）+ failures（失败）**；`render_experience` 区分渲染：失败条目输出"× 这些做法没奏效，避免重复"。
**验证**：微博任务失败 → "失败教训已记录: 25 步" ✓；重跑同任务 → 日志"已注入历史经验 **2** 条"（1 成功 + 1 失败）✓，且 **Jev 首步直接点「发现」（0.94，历史最高）**。

## 轮 11 · 四任务连测 + 语音闭环回归（03:03-03:10）

**四连测全部成功**（继首轮之后第二次全线通过）：

| 任务 | 结果 | 引擎 |
|---|---|---|
| 计算器 56+78 | 134 ✓ | qwen |
| 设置电池 | 94% ✓ | jev |
| 浏览器搜索"明天天气" | 25°/32° ✓（**输入类任务首胜**） | jev |
| 查天气（回归） | 22℃,多云 ✓ | jev |

- **语音闭环回归**：按音量下 → 语音条 + 搜狗语音面板 → 文字上屏 → 再按 → `voice: submitted` 提交 ✓。
- 结果格式化生效：多结果自动编号输出 ✓。

## 轮 12 · 语义复核 + 桥守护 + 微博封存（03:10-03:20）

- **qwen_verify 语义升级**：复核不只看"结果有无屏幕依据"，还看"**内容类型是否匹配任务**"（任务要"热搜榜"，普通帖子流不算）。
- **微博任务封存**：未登录内容限制 + 强反自动化 + 动态 UI（"回到顶部"浮钮遮挡等）→ 多轮攻坚仍不稳；**保留**"识别难局 - 诚实失败 - 教训积累"能力，不再专项死磕。App 要点更新为"搜索框路径（最可靠）"。
- **xgt-bridge 守护**：`supervisor-loop.sh` 增加桥进程看护（每分钟检查；桥 = 感知 + 操作容灾的关键组件，必须自愈）。
- **GitHub 推送受阻**（已解决，见轮 13）：代理故障（CONNECT 502 / TLS 中断）导致 r9+ 的 commits 本地积压；08:01 恢复补推成功。

## 轮 13 · 自我进化闭环 + GitHub 补推（03:16 → 08:01）

**自我进化**（回应"成功经验也要沉淀"）：任务成功后，qwen 从执行轨迹归纳一条「App 使用要点」（30 字内捷径），自动并入 `~/bridge/skills/apps.json`（与内置要点合并、外部可覆盖；同类任务下次开箱即用捷径）。
**验证**：时钟任务「明天最早的闹钟几点」→ **4 步 done「07:00」（纯 Jev）** ✓；apps.json 自动新增第一条自学习要点：

```json
{ "com.coloros.alarmclock": "打开时钟App后，点击底部闹钟标签页查看闹钟列表" }
```

**GitHub 补推**（用户发现"6 小时只见一轮提交"）：
- 根因：本机**直连 GitHub 超时**（10s 无响应），**必须走系统代理 127.0.0.1:7897**（实测 1.6s 200 OK）；02:45–03:20 代理抖动（CONNECT 502 / TLS 中断）→ r9-r12 push 失败积压，r13 未提交。
- 处置：重推 → **GitHub 上 6 个提交全部到位**（补推 3 个：r9-r10 / r11-r12 / r13）。
- 教训已入用户级 MEMORY：本机 push GitHub 必须走代理；排障勿清代理（清掉=唯一通路断）。

## 轮 14 · 日历任务泛化（08:04-08:06）

**验证**：全新 App（无预置要点）「打开日历看今天日程」→ **4 步 done（纯 Jev）**，回答「今天没有日程」✓。
**自我进化二次生效**：成功后自动沉淀 `"com.coloros.calendar": "打开日历后点击底部今日按钮，下滑查看日程"`（apps.json 现有 2 条自学习要点）。

## 事故复盘 · 03:19–08:01 会话静默卡死（用户发现「3:10 后没有记录」）

**现象**：03:19 起约 **4 小时 42 分钟**本会话零动作（无提交/无任务/无文件改动）；两端服务均正常、无报错。
**证据链（查全部客户端日志）**：
- 会话事件推送计数定格 **03:17:09**；最后一条沙箱命令（sleep 100 查结果）**03:18:41** 执行后无下文。
- 本会话累计推送事件 **40 万+**（工具帧 16 万+，节流后仅送出 ~4400）；**UI 渲染进程自 03:10 起持续 ~2.2GB / 85–96% CPU**。
- 手机端 console/supervisor/xgt-bridge 连续在跑（uptime 7h0m/4h52m/5h27m，无重启）；PC 守护心跳/内存监控/整点校验全部正常，无崩溃、无网络错误记录。
- 08:00–08:01 客户端整体重启（全进程换 PID）；08:01 用户消息唤醒会话 → 恢复。
**结论**：非手机/网络/模型问题——**超长单会话 + 巨量输出把客户端拖死，回合静默卡死且不自愈**（需重启或新消息唤醒）。
**对策**：① 短回合制（不再单会话连跑数小时；每轮结束即本地提交——不依赖网络、断点清晰）；② 每轮记录带时间戳，空档一眼可查；③ 再遇长时间无动静 → 重启客户端或发消息即可唤醒（本次验证有效）。

## 阶段小结（08:22）· 功能现状与下一步

**能干什么（真机验证）**：
- 三种入口：音量下语音速记（说话→再按一下执行）/ 浮球面板 / 网页控制台
- 自动操作手机完成任务：开 App、点按、滑动、输入文字、看屏幕（含微博类"黑盒"App 的界面树导出补盲）
- 质量保障：说"完成"前先复核（防吹牛）；失败教训自动入库、同类任务注入"别再做"；成功经验自动沉淀 App 要点（现 2 条：时钟/日历）；登录页/验证码自动停手；通知栏实时显示第 N 步
**实测通过**：设置-电池 / 计算器（56+78=134）/ 查天气 / 浏览器搜索（输入类）/ 时钟闹钟 / 日历 / 微博热搜（部分）；今日累计 30 次真机运行、19 次成功收尾。

**待迭代（按优先级）**：
- P0：微博热搜稳定复现；扩大 App 覆盖（微信等常用 App 实测 + 沉淀经验卡）
- P1：开工前"任务翻译"（去废话、补意图、明确"什么算完成"）；结果表述清洗（去掉屏幕杂讯）；设备档案（认识本机全部 App）
- P2：成功后自动归纳升级（卡片/规则规模化）；手机重启后自愈验证；长期稳定性

---

## 轮 15 · 任务规划 + 成功率专项（12:00-12:36）

**用户反馈**：最近两个自动化任务（开盘啦、华尔街见闻）没达到要求、路径奇怪——要求"先 qwen 理解问题、安排步骤可能性，再提高 Jev 效率，迭代成功率"。

**改造（mini_agent.py）**：
1. **开工前规划 `qwen_plan`**：任务原文 + 81 个已安装应用清单 → qwen 先纠正语音同音字（「胎盘/开盘了」→ 开盘啦）、从清单里挑目标 App（校验包名真实存在）、给出步骤计划 + 完成标准 + 备用方案；计划注入 Jev 与 qwen 的每一步提示词。
2. **包名解析修复 `resolve_package`**：原来 "com.xxx.app" 的尾段 `app` 会模糊命中 `com.jingdong.app.mall`（灾难：幻觉包名全被"矫正"成京东，连开 22 次空转）；现只匹配包名里"有意义的段"并排除通用词。
3. **滑动迷路守卫**：相同滑动连续 ≥4 次软提醒（若确实在正常滚动可继续）、≥7 次硬阻断（治"同一滑动刷 15 次"的迷路）。
4. **复核强化 `qwen_verify`**：新增条件④"主题/来源必须与任务点名的对象一致"（治"打开 Edge 看到行情数据就交差"）；复核与总结均使用规划后的任务理解（intent），不受错别字干扰。
5. **结果生成强化**：qwen_summary 只输出答案本身（编号列出实际内容，不给操作建议/坐标）。
6. **经验库卫生**：`learn_app_hint` 增加"包名必须在已安装清单"校验（防幻觉要点，如 com.wallstreetcn）；"无法完成"的任务不再沉淀成功经验；清理历史幻觉条目。

**实测对比（真机跑通）**：

| 任务 | 改造前 | 改造后 |
|---|---|---|
| 开盘啦「查最近炒作的热点」（含语音错字） | **25 步失败**（qwen 幻觉包名 → 被"矫正"成京东 → 连开 22 次） | **6 步 done**：正确识别「开盘啦」→ 打开 App → 读出真实热点 6 条（并购重组·新华传媒 +10.06% 等） |
| 「华尔街见闻的头条新闻」 | 13 步「未找到应用」（乱翻金融文件夹） | **18 步 done**：规划 → 用浏览器搜索 → 进官方 7x24 页 → 列出 4 条真实头条 |

**收官复测（12:36）**：开盘啦任务最终版 **3 步 done**（4 条热点）。本轮还实证拦截了一类新幻觉——模型**照抄旧经验的答案**（第 2 步 done 引用了上次结果里的股票名，复核判「屏幕中找不到依据」被驳回 ✓）；已在经验注入处加"不得照抄旧结果，作答以当前屏幕为准"提示。

**遗留/观察项**：浏览器搜索类任务里 Jev 仍会先"摸"几步再交 qwen（未阻断，可接受）；滑动守卫软提醒阈值（4 次）与措辞待更多样本验证；Jev 无"输入文字"动作（搜索类需 qwen 接管，属结构性设计）。

---

## 轮 16 · 微博热搜失败根因攻坚（13:00-13:45）

**用户反馈**：「嗯，去微博找一下热搜前五是什么？」25 步失败（用户自己跑的）→ 要求复测迭代 + 思考提高准确率。

**根因链（三层，全部实测定位）**：
1. **感知盲区（决定性）**：微博「发现」页的「微博热搜」榜单在无障碍树里是**一个合并节点**（content-desc 塞着全部约 12 条热搜词、无分隔符）；而「小工头」App 的树导出有两处截断——**深度上限 20**（该节点在第 22 层，被整棵剪掉）＋ **label 上限 50 字**（就算拿到也只留 2 条词）。→ 两个引擎都"看不见"榜单，在哪儿怎么点都白搭。
2. **qwen 吃到错误的事实来源（代码 bug）**：主循环"感知补盲"（桥树）只喂给了 Jev；qwen 提示词仍走 `render_tree(portal state)`——该页 portal 只有 5 个元素（纯底部导航）→ qwen 全程"只看到导航栏"。
3. **经验库污染（背景因素）**：成功经验里混着"失败话术"条目（如"未显示热搜内容…无法查看"被当成功保存）+ 滚动刷屏轨迹，注入后教坏模型（本轮顺带清理 15→13 条）。

**修复**：
- APK（EntryA11yService）：树导出 **深度 20→40、label 截断 50→220**（热搜 blob 全量可见，实测桥树立即出现完整词条）。
- mini_agent：新增 `render_elems()`——**qwen 提示词与 done 总结统一用"补盲后的元素清单"**（与 Jev 同源）；元素 label 截断 50→200。
- 总结重排：done 答案若 ≥60 字（原始拼接文本）→ 自动经 qwen_summary 整理成编号列表。
- 经验库卫生：save_experience 增加"失败话术"过滤 + 去 wait / 去连续滚动；learn_app_hint 要求"通用要点"（不写具体任务内容）。
- 微博经验卡重写（实测路径：榜单就在发现页顶部；不要搜索；页面被滚过先点「回到顶部」）。

**实测**：修复前 25 步失败（两引擎都"看不见"）→ **修复后 10 步 done**，答出真实热搜（游本昌去世 / 游本昌朋友圈置顶 / 长安启源Q06 / 罗永浩 / iPhoneDuo…）。

**提高准确率的思路（本轮沉淀）**：
1. **感知优先**：看不清=必败。凡"屏幕上有、树里没有"的内容都是头号 bug；出异常先做"截图 vs 元素清单"对账。
2. **单一事实来源**：所有环节（Jev / qwen / 总结 / 复核）必须吃同一份"补盲后"清单，避免某一路"半盲"。
3. **数据卫生**：经验库/卡片要防污染（失败话术、任务专属内容、滚动噪声）——坏样本比没样本更糟。
4. **答案成型**：读到原始数据后要有"成型"步骤（拼接 → 结构化列表），否则体验差。
5. **迷路熔断分级**：软提醒 → 换引擎 → 重置提示 → 硬阻断（本轮已具备四档）。


---

## 轮 17 · 微博热搜【真榜路径】修正（14:22-15:05）

**用户现场纠正**：「你看到的那个微博热搜界面是随机的，不是排行。我要的是排行，它在『更多热搜』里……然后『我的』第一栏再点『热搜』，这个才是一二三四五真正的热搜榜。」

**问题定性**：轮 16 后拿到的"热搜"内容（游本昌去世…）来自**发现页的热搜卡片**——那是**个性化/随机推荐**，不是官方排行。用户要的是带编号「1 2 3 4 5」的真榜。

**真机复现（逐屏核对）**：
1. 发现页 → 点热搜卡片右下角「更多热搜 >」（坐标 907,846）
2. 进入「微博热搜」页（RankPageActivity）→ **默认停在「我的」标签** =「热搜雷达，发现你关心的热点」（个性化流，非排行）
3. 点顶部标签栏第 2 个「热搜」（坐标 267,624）→ 出现「实时热点，每分钟更新一次」+ 带编号 1、2、3… 的榜单 → **这才是真排行**
   - 真榜结构：置顶（无热度值）+ 编号条目（名称 + 热度数值，如 1090896）+ 推广（无热度值，混排）
   - 编号与"顶/推广"标记均为**自绘图形，不在无障碍数据里**（必须靠热度值推断名次）

**修复（四件）**：
1. **App 经验卡升级：hint + spots（经验坐标注入）**——卡片支持 `spots:[label, x, y, [页面特征词]]`；主循环检测到特征词时把实测坐标作为**虚拟元素**注入元素清单（Jev/qwen 可直接选点）。微博卡写入三步路径 + 两坐标：更多热搜(907,846)、热搜标签(267,624)。防重复：坐标接近且同名元素已存在时跳过；手册卡被自动学习覆盖的问题同步治理（见 4）。
2. **排行提取 `render_rank_items`**——从屏幕元素提取「标题 + 同行热度数值」对，按热度值降序推断官方名次（置顶/推广无热度值 → 自动排除）；输出作为「官方排名候选」注入 qwen 汇总提示词。
3. **列表任务强制全量汇总**——`wants_multi_answer`（前N/哪些/排行/榜单…命中）时，done 的答案**强制过 qwen_summary**（Jev 的单条选择不算完整答案）；驳回提示同步要求"逐条列全（1. xxx 2. xxx）"。
4. **手册卡保护**——含 spots 的 App 卡视为人工维护，自动学习（learn_app_hint）不再覆盖其要点（治"微博首页顶部右侧为热搜入口"这类歪经验污染）；verify 增补"排行结构"条件（个性化流/雷达流不算排行）。

**部署事故与修复**：`tr -d \r`（单层引号）经远程 sh 解释为 `tr -d r` → **删光文件里所有字母 r**（"import"→"impot"）→ 语法错误。改为**本地转 LF 再 push**（不依赖远程转码）。教训：跨 shell 层命令别用裸 \r。

**端到端验证（6 步纯 Jev）**：
- open_app → 跳过广告 → 发现(0.99) → **更多热搜【经验坐标】(0.94)** → **热搜标签【经验坐标】(0.91)** → done
- 答案 `1. 中美元首华盛顿再次见面 / 2. 考研 / 3. 卫星互联网低轨26组卫星成功发射 / 4. 一笑倾城 王朱悦 / 5. 林绣茹 和离`
- 抓屏复核（RankPage 元素，热度降序）：**与输出逐条一致** ✓（置顶"习主席访问美国"、推广"小米18Pro开售"自动排除）

**对照（同任务三轮演进）**：
| 版本 | 结果 | 来源 |
|---|---|---|
| 轮 15 前 | 25 步失败 | 感知盲区（榜单被深度截断剪掉） |
| 轮 16 | 10 步 done（内容是随机推荐） | 发现页个性化卡片（❌ 非排行） |
| 轮 17 | 6 步 done + 真榜前5（热度降序） | 「更多热搜→热搜」真榜（✅） |

---

## 轮 18 · 命中 / 记账 / 加速 实测与三处补强（15:20-15:50）

**验证方法**：同一任务「嗯，去微博找热搜前五是什么？」连测 3 次（A/B/C，统一起点流程：回微博首页 → HOME → 提交），并与全天历史数据拉通对照。

**全天同任务数据（真实日志）**：

| 时段 | run | 耗时 | 步数 | 结果 |
|---|---|---|---|---|
| 摸索期（上午） | 00:27 / 02:13 / 02:18 | 82 / 88 / 117 秒 | 25 步×3 | ❌ 步数用尽 |
| 摸索期（中午） | 12:54 / 13:20 | 92 / 150 秒 | 25 步×2 | ❌ 步数用尽 |
| 修复后 | 13:35 → 13:38 | 49 → 33 秒 | 10 → 5 步 | ✅（随机榜内容） |
| 真榜修正 | 14:57 | 35.7 秒 | 6 步 | ✅ 真榜前 5 |
| 本轮 A | 15:24 | 34.4 秒 | 6 步 | ✅ |
| 本轮 B | 15:25 | 55.2 秒 | 13 步 | ✅（起点在 RankPage，绕路 7 步） |
| 本轮 C | 15:28 | 93.9 秒 | 7 步 | ✅（portal 掉线，桥兜底 + 2 次驳回） |

**三处补强**：
1. **"热搜排行标签"注入条件收紧**：whens 由 `["文娱","实时热点","热搜雷达"]` 改为 `["热搜雷达"]`——只有「我的」页（热搜雷达）才需要点「热搜」标签；「热搜」页不再重复注入（B 组曾因"文娱"二字在发现页/他处出现而误注入）。
2. **错题本悖论修复**：`save_experience` 成功后自动从 `failures.json` 移除同类任务——避免同任务"成功经验 + 失败教训"同时注入（对引擎而言自相矛盾）。
3. **容灾链路再验证**：C 组撞上 portal 服务掉线（ConnectionRefused，每步 3 次重试 ~4.5s 额外开销）→ 桥树自动顶上 → 任务仍成功；supervisor **15:31:06 检测 → 15:31:14 修复完成（8 秒）**。

**结论（对用户问题的回答口径）**：
- **第一次 vs 现在**：探索期 90~150 秒且失败 → 现在 33~36 秒成功（快约 3 倍，步数 25→6 省 76%，核心是从"没结果"到"有结果"）。
- **快慢的真正来源**是"机制"（翻译 + 卡片坐标 + 到页验收 + 记账），而非重复次数——B 组证明单纯重跑不一定更快（起点页面状态是最大波动源）。
- **三本账现状**：examples 15 条 / failures 5 条 / apps 5 条（全部在手机本地 `~/bridge/skills/`）。

---

# 迭代 20 轮 · 开盘啦 / 微博 / 新闻（任务驱动）

**目标**：更好用（覆盖面↑）、更快（步数/耗时↓）、更准确（答案正确率↑）
**方法**：每轮 = 设计任务 → 真机自动化 → 拉日志分析 → 修复 → 记录。任务池覆盖 3 个域 × 多任务。

| 轮 | 任务 | 结果 | 问题/修复 |
|---|---|---|---|
| r19 | 腾讯新闻·头条前3 | 9步/104s/✅3条 | **入口App进程死了**(15:49后)→桥树全超时；supervisor 加 App 进程看护（a11y toggle 拉起）|
| r20 | 腾讯新闻复测 | 2步/21s/✅ | 答案显示不全→**console 多行 DONE 解析 bug**（只取首行）→ 已修 |
| r21 | 腾讯新闻复测2 | 2步/26.5s/✅3条完整 | 桥恢复+多行修复生效：r19 104s → r21 26.5s |
| r22 | 今日头条·推荐前3（新App） | 6步/35.6s/✅3条 | 启动慢(open_app 重复+返回绕路4步)；答案真实 |
| r23 | 开盘啦·今日涨停家数 | 12步/68s/✅52家 | Jev 盲滚 7 步无果→qwen 绕 4 步；数据其实在首页第二格图标 |
| r24 | 开盘啦侦察（人工） | ✅路径确认 | **首页第1行第2个图标「市场情绪」(405,342)**→首屏「实际涨停 N 家」；写入手册卡+坐标 |
| r25 | 涨停家数复测（新卡） | 2步/19.8s/✅52家 | 卡+坐标生效：12步/68s → 2步/19.8s |
| r26 | 微博·热搜第十条 | 18步/77s/❌诚实失败 | 微博冷启动慢(open_app×5被BLOCK)；无序号→**榜单跨步累积器**（滚动收集）|
| r27 | 微博第十条复测（累积器） | 6步/47.7s/⚠️答案混乱 | qwen_summary 覆盖了 Jev 的合理答案；verify 名次依据与系统不一致 |
| r28 | 微博第十条复测2（系统锁） | 4步/26.4s/✅ | **榜单收集推进+系统锁**：滚动→累积≥N+2→系统直接给出第N条 |
| r29 | 开盘啦·龙虎榜 | 10步/48.5s/✅ | 起点在市场情绪详情页→Jev盲滚6步→qwen 返回首页→点底部「龙虎榜」tab→63只股票 |
| r30 | 龙虎榜复测（新卡） | 2步/21.6s/✅ | 起终点=龙虎榜页（页面恢复）；卡含底部导航坐标 |
| r31 | 微博·搜索"华为" | 25步/186s/❌步数耗尽 | Jev 不会输入（无 input_text 能力）→ 点搜索框又返回的横跳；qwen 误判页面 |
| r32 | 微博搜索复测（键盘切换） | 7步/43.1s/✅ | **键盘弹出→立即切 qwen**（决策前切换）+ SYSTEM_PROMPT 搜索流程：input_text→ENTER→读结果 |
| r33 | 腾讯新闻·点开第一条新闻 | 4步/61.6s/✅ | 详情+摘要（verify 首对象误判被修：取最后一个 JSON）|
| r34 | 今日头条·财经板块 | 18步/69.7s/⚠️ | 频道栏盲找（点财经tab实际生效但被误判无进展）；答案内容存疑 |
| r35 | 财经复测（新卡） | 4步/32.6s/✅2条 | **头条频道栏卡+坐标(445,313)**：18步→4步 |
| r36 | 微博热搜前五回归 | 21步/111.7s/⚠️ | 起点=搜索页残留→Jev 迷茫；无坐标注入（不在特征页）→ 答案来源不明确 |
| r37 | 微博热搜前五终回归 | 12步/60.6s/⚠️ | 复位首页后仍绕（Jev 在发现上循环）；**页面状态漂移是最大不确定性** |

---

## 20 轮迭代总结（r19-r38）· 开盘啦 / 微博 / 新闻

**总账**：17 个真机任务（跨 5 个 App）+ 6 个系统级修复 + 6 张经验卡（新增/强化）。

**修复清单（按价值排序）**：
1. **入口 App 进程看护**（supervisor）：App 进程死 8h 无人知（a11y 服务在列表 ≠ 进程活着）→ a11y toggle 拉起（实测 8 秒复活）＋桥树恢复。
2. **多行答案解析修复**（console）：DONE 多行答案只存首行 → 通知/历史显示不全（"头条前三条分别是："）→ 修复后完整。
3. **榜单系统**（agent 侧，系列）：跨步累积器（滚动收集）+ 收集推进提示 + "第N条"系统锁（系统确定答案，防模型猜错）→「热搜第十条」从 25 步失败 → 4 步给对。
4. **键盘弹出手动切换**：Jev 无输入能力（搜索类任务 25 步烧光）→ 键盘一出现立即切 qwen + 搜索流程提示 →「微博搜索华为」7 步成功。
5. **open_app 冷启动适配**：大 App 冷启动 20 秒（微博）→ 3 秒等待 + 3 次容错（原 2 次就被 BLOCK）。
6. **verify JSON 解析**：取最后一个含 ok 的对象（首对象误判导致正确结果被驳回）。
7. **经验卡（6 张）**：微博（真榜三步+底部导航坐标）、开盘啦（市场情绪/龙虎榜+坐标）、今日头条（频道栏+财经坐标）、腾讯新闻、Edge、天气/日历/时钟等。
8. **错题本悖论修复**（r18 遗产跟进）：成功后从 failures 移除同类任务。

**实测效率对照（同任务首测 vs 修复后）**：
| 任务 | 首次 | 修复后 |
|---|---|---|
| 腾讯新闻·头条前3 | 9步/104s | 2步/26.5s |
| 开盘啦·涨停家数 | 12步/68s | 2步/19.8s |
| 开盘啦·龙虎榜 | 10步/48.5s | 2步/21.6s |
| 今日头条·推荐前3 | 6步/35.6s | — |
| 今日头条·财经 | 18步/69.7s | 4步/32.6s |
| 微博·搜索华为 | 25步/186s❌ | 7步/43.1s |
| 微博·热搜第十条 | 18步/77s❌ | 4步/26.4s |

**核心教训（跨轮沉淀）**：
1. **页面状态漂移是最大不确定性**：App"恢复上次页面"行为让起点无法保证（33% 的任务受它干扰）→ 经验卡必须写"若不在目标页如何返航"。
2. **"看得到"→"选得对"是两个问题**：合并 blob、自绘图形、频道栏等场景需要"经验坐标"补丁（虚拟元素），坐标类经验卡是最有效的加速器。
3. **模型分工要匹配能力**：Jev 快但不会输入/难处理复杂页；键盘/列表/搜索场景主动切 qwen（而不是等"无进展 3 次"）。
4. **每一步都可能有"状态误解"**：完成判定（verify）、失败判定（无进展）、页面判定（在哪）都要"证据锚点"（特征词/系统数据），不能靠模型自觉。
