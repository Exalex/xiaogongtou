# 开发笔记 · 通宵迭代（2026-09-24）

> 任务：①创建 GitHub 版本 → ②持续迭代（每轮真机操作手机、发现问题、下一轮改进 UI/功能/架构）→ ③全过程记录到本文档。
> 规则：不打断、不询问，测试 → 反馈 → 改进 循环。

## 迭代看板

| 轮次 | 时间 | 主题 | 状态 | 验证 |
|---|---|---|---|---|
| 1 | 02:07 | GitHub 发布 + 密钥脱敏 | ✅ | 真机任务通过（jev=2）、`gh repo create Exalex/xiaogongtou` 推送成功 |
| 2 | 02:13 | 屏幕变化反馈 + 无效点击拦截（P0-2 前半） | ✅ | 微博任务复测：无效点击被 BLOCK（但暴露新问题） |
| 3 | 02:18 | wait 选项 / 阶段检查 / block 警告升级 | ✅ | 部署生效；发现"感知盲区"根因 |
| 4 | 02:22 | **感知补盲**：uiautomator root 桥（架构级） | ✅ | 微博任务 8 步 done（首胜），但暴露"done 幻觉" |
| 5 | 02:26 | **防幻觉复核**（done 验证） | 进行中 | 微博任务复测中 |

---

## 轮 1 · GitHub 版本 + 密钥脱敏（02:07）

**背景**：GitHub 发布前全仓扫描发现真实密钥（TypeSafe Jev key、portal token）硬编码在多处。

**动作**：
1. 手机端 `mini_agent.py`：`JEV_KEY` / `PORTAL_TOKEN` 改为 **`~/bridge/secrets.json` 读取**（不进 git），环境变量优先。
2. `jev_probe.py` / `pc_portal.py` / `portal_probe.py`：密钥默认值改为空 + 环境变量（补了 `import os`）。
3. 入口 APK：`Api.java` 的 portal token 改为引用 `Secrets.PORTAL_TOKEN`；**`Secrets.java` 由 build.py 从 `entry/local_secrets.json`（gitignored）生成**到构建目录（源码树不含密钥）。
4. 文档脱敏（`项目完整文档.md` 2 处）。
5. `.gitignore`（local_secrets / build / dist / logs / apk / .workbuddy）。
6. `README.md`（架构图 + 快速开始 + 文档索引）。
7. **发布**：`gh repo create xiaogongtou --public --push` → https://github.com/Exalex/xiaogongtou

**验证**：手机侧部署 `~/bridge/secrets.json` + 新 APK；跑「回到桌面」任务 → `done`（jev=2）——脱敏后 Jev 与 portal 链路均正常。

## 轮 2 · 屏幕变化反馈 + 无效点击拦截（02:13）

**背景**（微博案病根）：qwen 连点同一坐标 5 次不自知——它收到的反馈只有"执行成功"，**不知道"屏幕没变"**。

**动作**（`mini_agent.py`）：
1. 每步开头用"屏幕 hash 对比"**补记上一步效果**："（系统补充）上一步动作已执行，但屏幕没有任何变化——可能没点中/无效"。
2. **无效点击计数**：同一 tap 连续 2 次"无效果" → 并入硬阻断体系（拒绝执行 + 强反馈）。

**验证**：微博任务复测 → 无效点击确实被 BLOCK（step 9-25 全部被拒）✓；**但暴露新问题**：qwen 被 BLOCK 后仍反复提交同一动作 17 次（不改策略）。

## 轮 3 · 加载等待 + 阶段检查 + 警告升级（02:18）

**动作**：
1. **Jev 增加 `wait` 选项**（"页面正在加载"场景）——乱点治不了，先学会等。
2. **每 6 步"阶段检查"提示**："请先看屏幕：任务要求的信息是否已出现？已出现 → 立即 done"。
3. **连续被报复 ≥3 次**时警告升级："坐标已禁用；若认为任务已完成请直接 done"。
4. 升级 qwen 的交接提示追加"若屏幕上已有信息，请直接 done"。

**验证**：微博任务复测 → qwen 开始用 wait/swipe/返回/open_app 组合尝试（比死循环进步）；**但任务仍失败**——复盘发现：**qwen 说"内容区域空白"不是它笨，是真的看不到**！

## 轮 4 · 感知补盲：uiautomator root 桥（02:22）★★★

**根因发现**（本夜最大发现）：
- 同一时刻对比：**uiautomator dump 能看到微博信息流内容**（163 节点 / 16 文本），**portal 的 a11y 树只有 5 个节点**（只剩底部导航）。
- 原因：**portal 的 AccessibilityService 未开 `FLAG_INCLUDE_NOT_IMPORTANT_VIEWS`**（capabilities=161 里无 0x2），微博标记为"不重要"的内容区被过滤。
- 结论：**agent 一直以来是"瞎"的**（读不到信息流内容）——不是模型笨。微博案、部分"内容读不到"类失败的真正根因。

**架构方案（不动 portal）**：新增 **root 桥 `uidump-bridge.sh`**：
- root 常驻小脚本：轮询 `~/bridge/.uidump_req` → `uiautomator dump` → 写到 `~/bridge/.uidump.xml`（chown 给 Termux）。
- `mini_agent.py`：每步若 portal 树 **<8 个元素** → 自动请求补盲，用 uiautomator 完整树替换（坐标体系相同，可直接点击）。

**验证**：微博任务（portal 恰好掉线 + 树为空）→ **首次 done！8 步**（portal 0 → uiautomator 11 元素）。**但**：结果"热搜前5条：中国男篮vs日本男篮×5"经比对 `.uidump.xml` 铁证，**屏幕上是"推荐"信息流，没有任何男篮内容——qwen 编的（幻觉）**。

## 轮 5 · 防幻觉复核：done 验证（02:26）

**背景**：轮 4 的"假成功"——qwen 未完成任务但输出编造答案。
**动作**：
1. 新增 `qwen_verify(task, summary, screen_text)`：done 时让 qwen 自查"结果的关键信息能否在屏幕上找到依据"。
2. **驳回机制**：复核不通过 → 拒收 done 并把驳回理由回传引擎（要求继续操作，或如实报告"无法完成：原因"）；最多驳回 2 次防死循环。
3. "无法完成"开头的 summary 直接放行（**如实失败是合法结局**）。

**验证**：（本轮进行中——见下）

